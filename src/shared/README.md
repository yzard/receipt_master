# Shared mobile application

Shared Flutter code and resources for Android and iOS. Platform projects are in
`../android`, `../ios` and the existing Linux development host in `../linux`.
Use the repository root README and build entrypoints.
