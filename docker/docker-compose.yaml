name: receipt-master-ocr
x-gpu: &gpu
  init: true
  gpus: all
  devices:
    - /dev/nvidia-uvm:/dev/nvidia-uvm
    - /dev/nvidia-uvm-tools:/dev/nvidia-uvm-tools
  shm_size: 2gb
  stop_grace_period: 40s
  healthcheck:
    test: [CMD, python3, -c, "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=5)"]
    interval: 10s
    timeout: 6s
    retries: 60
    start_period: 60s
services:
  backend_ocr:
    <<: *gpu
    restart: unless-stopped
    image: receipt-master-backend-ocr:local
    environment:
      PUID: ${PUID:?Set PUID to the host user ID}
      GUID: ${GUID:?Set GUID to the host group ID}
    volumes:
      - ../playground/backend_ocr:/data:ro
    networks: [inference]
  backend_api:
    init: true
    stop_grace_period: 40s
    healthcheck:
      test: [CMD, curl, --fail, --silent, http://127.0.0.1:8000/health]
      interval: 10s
      timeout: 6s
      retries: 10
    image: receipt-master-backend-api:local
    environment:
      PUID: ${PUID:?Set PUID to the host user ID}
      GUID: ${GUID:?Set GUID to the host group ID}
    ports: ["0.0.0.0:5000:8000"]
    volumes:
      - ../build/mobile:/artifacts:ro
      - ../playground/backend_api:/data
    networks: [inference, ingress]
networks:
  inference:
    internal: true
  ingress: {}
